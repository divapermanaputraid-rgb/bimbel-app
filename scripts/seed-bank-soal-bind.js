// seed-bank-soal-bind.js — reads JSON, generates SQL migration
const fs = require('fs');
const path = require('path');

function escapeSql(str) {
  if (str === null || str === undefined) return 'NULL';
  return "'" + String(str).replace(/'/g, "''") + "'";
}
function escapeJson(obj) {
  if (obj === null || obj === undefined) return 'NULL';
  return "'" + JSON.stringify(obj).replace(/'/g, "''") + "'::jsonb";
}

const files = [
  'data/bank-soal-bind-k1.json',
  'data/bank-soal-bind-k2.json',
  'data/bank-soal-bind-k3.json',
  'data/bank-soal-bind-k6.json',
];

const rows = [];

for (const file of files) {
  const data = JSON.parse(fs.readFileSync(path.join(__dirname, '..', file), 'utf8'));
  const subjectId = `k${data.kelas}-bind`;
  for (const unit of data.units) {
    for (const soal of unit.soal) {
      const tipe = soal.tipe === 'truefalse' ? 'true_false'
        : soal.tipe === 'fill' ? 'fill_blank'
        : soal.tipe === 'match' ? 'match'
        : 'choose_correct';
      const pilihan = soal.pilihan ?? (soal.pasangan ? Object.keys(soal.pasangan) : ['Benar', 'Salah']);
      const jawabanBenar = soal.tipe === 'truefalse' ? (soal.jawaban === true ? 'Benar' : 'Salah')
        : soal.tipe === 'match' ? soal.jawaban
        : soal.jawaban;
      rows.push(`(
  ${escapeSql(soal.id)},
  ${escapeSql(unit.unit_id)},
  ${escapeSql(tipe)},
  ${escapeSql(soal.pertanyaan)},
  ${escapeJson(pilihan)},
  ${escapeJson(jawabanBenar)},
  1,
  10
)`);
    }
  }
}

const sql = `-- seed-bank-soal-bind.sql — generated by seed-bank-soal-bind.js
-- 110 soal B.Indonesia: K1(24) + K2(24) + K3(32) + K6(30)

DELETE FROM public.questions WHERE material_id LIKE 'k1-bind-%' OR material_id LIKE 'k2-bind-%' OR material_id LIKE 'k3-bind-%' OR material_id LIKE 'k6-bind-%';

INSERT INTO public.questions (
  id, material_id, tipe, soal, pilihan, jawaban_benar, level, xp
) VALUES
${rows.join(',\n')};
`;

const outPath = path.join(__dirname, '../supabase/migrations/036_seed_bank_soal_bind.sql');
fs.writeFileSync(outPath, sql, 'utf8');
console.log(`Written: ${outPath} (${rows.length} rows)`);
